// Test: 17-multimodal.md:489

main {
    main {
        // Default max 10 media per request
        let images = [media("img1.png"), media("img2.png"), media("img3.png")]
        let result = llm act "Analyze these images" media(images)
    }
}