// Test: 17-multimodal.md:460

main {
    main {
        // Small image (<1MB): inline storage
        let small_img = media("icon.png")

        // Large image (>=1MB): automatic external storage
        let large_img = media("high_res_photo.png")
    }
}