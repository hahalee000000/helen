// Test: 17-multimodal.md:186

main {
    main {
        let result = llm act "Describe this image in detail"
            media(img)
            on_chunk fn(chunk) {
                print(chunk, flush=false)
            }
            on_complete fn(full_text) {
                print("\nDone!")
            }
    }
}