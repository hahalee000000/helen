// Test: 17-multimodal.md:322

agent Claude分析 {
    main {
        let img = media("chart.png")
        
        # 使用内置 Claude 适配器 — 一行搞定
        let result = llm act "分析这个图表"
            media(img)
            on_media fn(parts, provider) { 转Claude格式(parts) }
        
        print(result)
    }
}