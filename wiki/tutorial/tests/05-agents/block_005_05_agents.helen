// Test: 05-agents.md:116

// === Context from preceding blocks ===
agent Greeter(user_name: str, user_id: int) {
}

// === Test code ===
agent SmartTranslator {
    description "High-quality translation"
    model "gpt-4"
    prompt "Translate carefully..."
}