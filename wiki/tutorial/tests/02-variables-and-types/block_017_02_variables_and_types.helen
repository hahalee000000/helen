// Test: 02-variables-and-types.md:253

main {
    main {
        let arr = [1, 2, 3]
        arr[0] = 10  // ✅ arr becomes [10, 2, 3]
        arr[1] = 20  // ✅ arr becomes [10, 20, 3]

        // Dynamic index
        let i = 2
        arr[i] = 30  // ✅ arr becomes [10, 20, 30]
    }
}