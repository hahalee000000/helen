// Test: 04-control-flow.md:113

// 安全的列表访问
let items = [1, 2, 3]
let first = len(items) > 0 && items[0]  // 避免空列表错误

// 缓存检查
let cached = cache.get(key)
let result = cached != null || computeExpensive()

// 权限检查
let canAccess = isLoggedIn() && hasPermission("admin")