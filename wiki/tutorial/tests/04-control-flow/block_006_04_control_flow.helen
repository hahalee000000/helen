// Test: 04-control-flow.md:111

main {
    main {
        // && has higher precedence than ||
        let result = a || b && c  // Equivalent to a || (b && c)

        // Use parentheses for clarity
        let result2 = (a || b) && c  // Explicit grouping
    }
}