// Test: 09-python-ffi.md:295
// @skip: needs Python module import

import "json" as json

main {
    // Create data
    let users = [
        {"name": "Alice", "age": 30},
        {"name": "Bob", "age": 25},
        {"name": "Charlie", "age": 35}
    ]
    
    // Serialize to JSON
    let json_data = json.dumps(users)
    print(json_data)
    
    // Parse JSON
    let parsed = json.loads(json_data)
    for user in parsed {
        print(user["name"] + " is " + str(user["age"]) + " years old")
    }
}