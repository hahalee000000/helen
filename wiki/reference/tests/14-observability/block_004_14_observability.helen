// Test: 14-observability.md:80

main {
    main {
        trace_on()
        let x = compute_value()
        let y = transform(x)
        trace_off()

        let trace = get_trace(10)
        print(trace)
    }
}