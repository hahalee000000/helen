// Test: 04-control-flow.md:123

main {
    main {
        // Safe list access
        let items = [1, 2, 3]
        let first = len(items) > 0 && items[0]  // Avoid empty list errors

        // Cache check
        let cached = cache.get(key)
        let result = cached != null || computeExpensive()

        // Permission check
        let canAccess = isLoggedIn() && hasPermission("admin")
    }
}