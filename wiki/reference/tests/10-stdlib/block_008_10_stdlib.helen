// Test: 10-stdlib.md:201

main {
    main {
        // Match
        let m = regex_match(r"\d+", "123abc")
        print(m.match)                // "123"

        // Search
        let s = regex_search(r"\d+", "abc123def")
        print(s.match)                // "123"

        // Replace
        regex_replace(r"\d+", "abc123def", "NUM")
        // "abcNUMdef"

        // Split
        regex_split(r"\s+", "a  b  c")
        // ["a", "b", "c"]

        // Find all
        regex_findall(r"\d+", "a1b2c3")
        // ["1", "2", "3"]
    }
}