// Test: 17-multimodal.md:474

main {
    main {
        try {
            let img = media("possibly_nonexistent_file.png")
            let result = llm act "Analyze" media(img)
        } catch err {
            print("Media loading failed: " + err.message)
        }
    }
}