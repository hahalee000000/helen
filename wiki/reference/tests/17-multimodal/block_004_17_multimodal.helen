// Test: 17-multimodal.md:87

agent Claude媒体处理 {
    main {
        let img = media("diagram.png")
        
        // Recommended: use built-in format adapter (one line)
        let result = llm act "Explain this diagram" 
            media(img)
            on_media fn(parts, provider) { return 转Claude格式(parts) }
    }
}