// Test: 05-agents.md:142

// === Context from preceding blocks ===
agent Greeter(user_name: str, user_id: int) {
}

// === Test code ===
agent Interviewer {
    description "Conduct an interview"
    max-turns 5    // Maximum 5 turns of conversation
    prompt "Ask follow-up questions..."
}