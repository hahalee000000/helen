// Test: 05-agents.md:699

agent Summarizer {
    description "Summarize text"
    prompt "Summarize the following:"
}

main {
    main {
        let text = "Long article content here..."
        let summary = Summarizer(text)
        print(summary)
    }
}