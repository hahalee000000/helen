// Test: 05-agents.md:555

main {
    main {
        // Text search (default)
        search_files("src/", "TODO")

        // Regex search
        search_files("src/", "def \\w+Handler", regex=true)

        // Case-insensitive
        search_files("docs/", "warning", case_sensitive=false)
    }
}